<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= service.name %> - Xerox E-Commerce</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <%- include('../partials/header') %>

        <main class="container my-5">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/">Home</a></li>
                    <li class="breadcrumb-item"><a href="/xerox">Xerox Services</a></li>
                    <li class="breadcrumb-item active" aria-current="page">
                        <%= service.name %>
                    </li>
                </ol>
            </nav>

            <div class="row">
                <div class="col-md-8">
                    <h1 class="mb-3">
                        <%= service.name %>
                    </h1>
                    <div class="mb-4">
                        <span class="badge bg-primary"><%= service.category %></span>
                        <% if (service.isColor) { %>
                            <span class="badge bg-success ms-2">Color</span>
                            <% } else { %>
                                <span class="badge bg-secondary ms-2">Black & White</span>
                                <% } %>
                    </div>

                    <div class="card mb-4">
                        <div class="card-body">
                            <h5 class="card-title">Description</h5>
                            <p class="card-text">
                                <%= service.description %>
                            </p>

                            <div class="row mt-4">
                                <div class="col-md-6">
                                    <h5>Service Details</h5>
                                    <ul class="list-group list-group-flush">
                                        <li class="list-group-item d-flex justify-content-between">
                                            <span>Price:</span>
                                            <span class="fw-bold">$<%= service.price.toFixed(2) %> per <%= service.priceUnit %></span>
                                        </li>
                                        <li class="list-group-item d-flex justify-content-between">
                                            <span>Paper Size:</span>
                                            <span><%= service.paperSize %></span>
                                        </li>
                                        <li class="list-group-item d-flex justify-content-between">
                                            <span>Estimated Time:</span>
                                            <span><%= service.estimatedTime %></span>
                                        </li>
                                    </ul>
                                </div>

                                <div class="col-md-6">
                                    <h5>Additional Options</h5>
                                    <% if (service.additionalOptions && service.additionalOptions.length > 0) { %>
                                        <ul class="list-group list-group-flush">
                                            <% service.additionalOptions.forEach(option => { %>
                                                <li class="list-group-item d-flex justify-content-between">
                                                    <span><%= option.name %></span>
                                                    <span>$<%= option.price.toFixed(2) %></span>
                                                </li>
                                                <% }); %>
                                        </ul>
                                        <% } else { %>
                                            <p>No additional options available for this service.</p>
                                            <% } %>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="d-grid gap-2 d-md-flex mt-4">
                        <a href="/xerox/upload" class="btn btn-primary me-md-2">Upload PDF for Printing</a>
                        <a href="/xerox" class="btn btn-outline-secondary">Back to Services</a>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="card mb-4">
                        <% if (service.image) { %>
                            <img src="<%= service.image %>" class="card-img-top" alt="<%= service.name %>">
                            <% } else { %>
                                <div class="card-img-top text-center py-5 bg-light">
                                    <i class="bi bi-printer display-1"></i>
                                </div>
                                <% } %>
                                    <div class="card-body">
                                        <h5 class="card-title">Order this Service</h5>
                                        <p class="card-text">Get started by uploading your PDF file for printing with this service.</p>
                                        <a href="/xerox/upload" class="btn btn-primary d-block">Upload PDF</a>
                                    </div>
                    </div>

                    <div class="card mb-4">
                        <div class="card-body">
                            <h5 class="card-title">Need Help?</h5>
                            <p class="card-text">Contact our customer service for assistance with your printing needs.</p>
                            <ul class="list-unstyled">
                                <li><i class="bi bi-telephone me-2"></i> +1 (555) 123-4567</li>
                                <li><i class="bi bi-envelope me-2"></i> support@xerox-ecommerce.com</li>
                                <li><i class="bi bi-clock me-2"></i> Mon-Fri: 9am-6pm</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </main>

        <%- include('../partials/footer') %>

            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>